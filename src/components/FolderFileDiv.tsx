import React, { useState } from "react";
import Draggable from "react-draggable";
import { Resizable } from "react-resizable";
const FolderFileDiv = () => {
  return <div className="flex-1 bg-slate-600"></div>;
};

export default FolderFileDiv;
